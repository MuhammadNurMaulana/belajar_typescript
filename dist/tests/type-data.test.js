"use strict";
describe("Type Data", () => {
    it("should Type Data", () => {
        const name = "Muhammad Nur Maulana";
        const age = 21;
        const isVip = true;
        expect(name).toBe("Muhammad Nur Maulana");
        expect(age).toBe(21);
        expect(isVip).toBe(true);
    });
});
